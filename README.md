# Content Verification Service

Simple, minimal project scaffold for content verification: text + image classification + fusion + policy decision engine.

Run the example:

```bash
python main.py
``` 

